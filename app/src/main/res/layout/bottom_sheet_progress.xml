<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                   xmlns:application="http://schemas.android.com/apk/res-auto"
                                                   android:layout_width="match_parent"
                                                   android:paddingBottom="42dp"
                                                   application:layout_behavior="@string/bottom_sheet_behavior"
                                                   android:layout_height="wrap_content">

    <ProgressBar android:layout_width="match_parent" android:layout_height="wrap_content"
                 android:indeterminate="true"
                 application:layout_constraintLeft_toLeftOf="parent"
                 application:layout_constraintTop_toTopOf="parent"
                 android:id="@+id/progressbar"
                 style="@style/Widget.AppCompat.ProgressBar.Horizontal"/>

    <TextView android:layout_width="0dp" android:layout_height="wrap_content"
              application:layout_constraintRight_toLeftOf="@id/iv_close"
              android:text="Loading..."
              application:layout_constraintLeft_toLeftOf="parent"
              application:layout_constraintTop_toBottomOf="@id/progressbar"
              android:id="@+id/tv_message"
              android:padding="8dp"
              android:textSize="18sp"/>

    <ImageView android:layout_width="42dp" android:layout_height="42dp"
               android:src="@drawable/ic_twotone_close_24px"
               application:layout_constraintTop_toBottomOf="@id/progressbar"
               android:id="@+id/iv_close"
               application:layout_constraintRight_toRightOf="parent"
               android:padding="8dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>